# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
*.local

# vite-plugin-pwa's dev-mode service worker scratch directory
dev-dist/

# Vite / tooling caches
.vite/
node_modules/.vite/
.cache/
*.tsbuildinfo

# Cloudflare / wrangler
.wrangler/
.dev.vars
.dev.vars.*

# Env files
.env
.env.*
!.env.example

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editors
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Test / coverage
coverage/
.nyc_output/

# Claude agent worktrees and session files
.claude/*
# ...but the project hook config is shared, not personal
!.claude/settings.json
# ...and so are project skills — lazily-loaded guidance, versioned with the code
!.claude/skills/


# Throwaway per-seed test files written by `pnpm run vet:seeds` (scripts/vet-seed-pool.mts),
# and the results file it appends to. Both are scratch for a seed search.
test/procgen/vet-seed-*.test.ts
seed-vetting*.jsonl
