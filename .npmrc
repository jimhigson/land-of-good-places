# Registry hiccups are common enough to be worth retrying. These carry the
# retry behaviour that used to be spelled out as flags on `npm ci` in every
# workflow; pnpm reads them from here, so CI just runs
# `pnpm install --frozen-lockfile`.
fetch-retries=5
fetch-retry-maxtimeout=120000
