# HANDOFF — coplanar-clear

Branch `fix/coplanar-clear`, off `origin/feat/procgen-on-sphere` @ e982430b.
Goal: `pnpm run check:coplanar` green on the base's pool seeds, each finding fixed at its cause
(delete hidden face / own phase / stop sharing an expression). Never add or widen a baseline entry.
Held back (need Jim's ruling): rail race `trestle-branches-lower|upper` fork caps, duck bar alert sleeve.

## Status
- [ ] base run of check:coplanar recorded
